import React, { useState } from "react";
import {
  AppBar,
  Toolbar,
  Link,
  Box,
  Button,
  Menu,
  MenuItem,
  Container,
  Tooltip,
} from "@mui/material";

const Home = () => {  
  return (
    <AppBar
      position="static"
      className="appbar"
      sx={{ backgroundColor: "common.black" }}
    >
      <Container maxWidth="xl">       
        <Box>sfsdfds</Box>
      </Container>
    </AppBar>
  );
};
export default Home;
